version: '3.8'

services:
  app:
    image: syed855/yii2-app:latest
    ports:
      - "8000:80"
    deploy:
      replicas: 1
      update_config:
        parallelism: 1
        delay: 10s
      restart_policy:
        condition: on-failure
